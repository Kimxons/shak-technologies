# Copilot Instructions

## Project Guidelines
- The user wants search logic to be centralized and reused across screens/modules instead of duplicated per-field implementations.
- View files should use the actual module name (not `Index`) for new module pages.